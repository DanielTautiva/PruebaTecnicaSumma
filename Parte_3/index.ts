import { AgentsFactory } from "./src/infrastructure/agentsFactory";

let agentType = "A"; // Cambiar según el agente que desees crear
let size = 4; // Cambiar según el tamaño de la escalera que desees

AgentsFactory.createAgent(agentType, size);

// agentType = "B"; 
// size = 4; 

// AgentsFactory.createAgent(agentType, size);

// let agentType = "C"; 
// let size = 4; 

// AgentsFactory.createAgent(agentType, size);